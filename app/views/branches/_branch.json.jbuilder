json.extract! branch, :id, :main, :name, :cnpj, :membership_id, :zip_code, :neighborhood, :street, :number, :state, :state_registration, :created_at, :updated_at
json.url branch_url(branch, format: :json)
