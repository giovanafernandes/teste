<div class="row window">
    <div class="col-md-12">
        <p id="notice"><%= notice %></p>

        <div class="row">
            <div class="col-12">
                <%= link_to employees_path, class: "link-plusing", style: "float: left;margin-right: 15px;" do %>
                  <img src="<%= image_url("back.svg") %>" width="30">
                <% end %>
                <h5 class="table-title">Novo Funcionário</h5>
            </div>
        </div>
        <div class="row">
            <div class="col-12">
                <%= react_component 'EmployeeForm', authenticity_token: form_authenticity_token, employee: @employee, jobs: @jobs, branch_id: @branch_id %>
            </div>
        </div>
    </div>
</div>
